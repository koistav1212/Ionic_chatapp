{
  "name": "ionic_chatapp",
  "version": "1.0.0",
  "description": "new_app",
  "engines": {
      "node": "18.12.0"
  },
  "scripts": {
      "start": "concurrently \"npm run server\" \"npm run client\"",
      "server": "cd server && npm start",
      "client": "cd client && npm start",
      "build": "npm install && cd client && npm run build && cd ../server && npm run build",
      "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
      "concurrently": "^8.2.1"
  }
}
